# Copy this file to `.env.local` and fill values before starting the servers.
# Example values below are placeholders — replace with real credentials.
OPENAI_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
FRONTEND_URL=http://localhost:3001
PORT=4000
# Optional: your database connection string if used by the server
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
